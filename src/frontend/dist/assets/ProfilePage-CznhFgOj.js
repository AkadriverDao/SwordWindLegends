var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
import { c as createLucideIcon, j as jsxRuntimeExports, A as ArrowLeft, S as SquarePen, E as Eye, M as MarkdownRenderer, e as encode, T as Text, i as isV4ResponseBody, u as useActor, r as reactExports, a as useQueryClient, H as HttpAgent, L as Link, b as ue, d as useCreateArticle, f as useUpdateArticle, g as useSaveUserProfile, U as User, X, h as reactDomExports, k as useGetCallerUserProfile, l as useGetMyArticles, m as useGetMiningLoggingEnabled, n as ArticleList, B as BookOpen } from "./index-C0vAz2PO.js";
import { I as Image, S as Save } from "./save-Bqc25KJ7.js";
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$b = [
  [
    "path",
    { d: "M6 12h9a4 4 0 0 1 0 8H7a1 1 0 0 1-1-1V5a1 1 0 0 1 1-1h7a4 4 0 0 1 0 8", key: "mg9rjx" }
  ]
];
const Bold = createLucideIcon("bold", __iconNode$b);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$a = [
  ["path", { d: "m16 18 6-6-6-6", key: "eg8j8" }],
  ["path", { d: "m8 6-6 6 6 6", key: "ppft3o" }]
];
const Code = createLucideIcon("code", __iconNode$a);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$9 = [
  ["path", { d: "M10 12.5 8 15l2 2.5", key: "1tg20x" }],
  ["path", { d: "m14 12.5 2 2.5-2 2.5", key: "yinavb" }],
  ["path", { d: "M14 2v4a2 2 0 0 0 2 2h4", key: "tnqrlb" }],
  ["path", { d: "M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7z", key: "1mlx9k" }]
];
const FileCode = createLucideIcon("file-code", __iconNode$9);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$8 = [
  ["path", { d: "M4 12h8", key: "17cfdx" }],
  ["path", { d: "M4 18V6", key: "1rz3zl" }],
  ["path", { d: "M12 18V6", key: "zqpxq5" }],
  ["path", { d: "m17 12 3-2v8", key: "1hhhft" }]
];
const Heading1 = createLucideIcon("heading-1", __iconNode$8);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$7 = [
  ["path", { d: "M4 12h8", key: "17cfdx" }],
  ["path", { d: "M4 18V6", key: "1rz3zl" }],
  ["path", { d: "M12 18V6", key: "zqpxq5" }],
  ["path", { d: "M21 18h-4c0-4 4-3 4-6 0-1.5-2-2.5-4-1", key: "9jr5yi" }]
];
const Heading2 = createLucideIcon("heading-2", __iconNode$7);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$6 = [
  ["path", { d: "M4 12h8", key: "17cfdx" }],
  ["path", { d: "M4 18V6", key: "1rz3zl" }],
  ["path", { d: "M12 18V6", key: "zqpxq5" }],
  ["path", { d: "M17.5 10.5c1.7-1 3.5 0 3.5 1.5a2 2 0 0 1-2 2", key: "68ncm8" }],
  ["path", { d: "M17 17.5c2 1.5 4 .3 4-1.5a2 2 0 0 0-2-2", key: "1ejuhz" }]
];
const Heading3 = createLucideIcon("heading-3", __iconNode$6);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$5 = [
  ["line", { x1: "19", x2: "10", y1: "4", y2: "4", key: "15jd3p" }],
  ["line", { x1: "14", x2: "5", y1: "20", y2: "20", key: "bu0au3" }],
  ["line", { x1: "15", x2: "9", y1: "4", y2: "20", key: "uljnxc" }]
];
const Italic = createLucideIcon("italic", __iconNode$5);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$4 = [["path", { d: "M21 12a9 9 0 1 1-6.219-8.56", key: "13zald" }]];
const LoaderCircle = createLucideIcon("loader-circle", __iconNode$4);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$3 = [
  [
    "path",
    {
      d: "M15.707 21.293a1 1 0 0 1-1.414 0l-1.586-1.586a1 1 0 0 1 0-1.414l5.586-5.586a1 1 0 0 1 1.414 0l1.586 1.586a1 1 0 0 1 0 1.414z",
      key: "nt11vn"
    }
  ],
  [
    "path",
    {
      d: "m18 13-1.375-6.874a1 1 0 0 0-.746-.776L3.235 2.028a1 1 0 0 0-1.207 1.207L5.35 15.879a1 1 0 0 0 .776.746L13 18",
      key: "15qc1e"
    }
  ],
  ["path", { d: "m2.3 2.3 7.286 7.286", key: "1wuzzi" }],
  ["circle", { cx: "11", cy: "11", r: "2", key: "xmgehs" }]
];
const PenTool = createLucideIcon("pen-tool", __iconNode$3);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$2 = [
  [
    "path",
    {
      d: "M16 3a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2 1 1 0 0 1 1 1v1a2 2 0 0 1-2 2 1 1 0 0 0-1 1v2a1 1 0 0 0 1 1 6 6 0 0 0 6-6V5a2 2 0 0 0-2-2z",
      key: "rib7q0"
    }
  ],
  [
    "path",
    {
      d: "M5 3a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2 1 1 0 0 1 1 1v1a2 2 0 0 1-2 2 1 1 0 0 0-1 1v2a1 1 0 0 0 1 1 6 6 0 0 0 6-6V5a2 2 0 0 0-2-2z",
      key: "1ymkrd"
    }
  ]
];
const Quote = createLucideIcon("quote", __iconNode$2);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$1 = [
  ["path", { d: "M16 4H9a3 3 0 0 0-2.83 4", key: "43sutm" }],
  ["path", { d: "M14 12a4 4 0 0 1 0 8H6", key: "nlfj13" }],
  ["line", { x1: "4", x2: "20", y1: "12", y2: "12", key: "1e0a9i" }]
];
const Strikethrough = createLucideIcon("strikethrough", __iconNode$1);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode = [
  [
    "path",
    {
      d: "M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z",
      key: "vktsd0"
    }
  ],
  ["circle", { cx: "7.5", cy: "7.5", r: ".5", fill: "currentColor", key: "kqv944" }]
];
const Tag = createLucideIcon("tag", __iconNode);
const AVAILABLE_CATEGORIES = [
  { value: "技术分享", label: "技术分享", emoji: "💻" },
  { value: "学习笔记", label: "学习笔记", emoji: "📚" },
  { value: "人生经历", label: "人生经历", emoji: "💭" }
];
function CategorySelector({
  selectedCategories,
  onCategoryToggle
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "label",
      {
        htmlFor: "category-selector",
        className: "flex items-center text-lg font-bold text-dark-text mb-3",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Tag, { size: 20, className: "mr-2" }),
          "🏷️ 分类标签"
        ]
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { id: "category-selector", className: "flex flex-wrap gap-2", children: AVAILABLE_CATEGORIES.map((category) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "button",
      {
        type: "button",
        onClick: () => onCategoryToggle(category.value),
        className: `inline-flex items-center px-3 py-1.5 rounded-lg text-sm font-medium transition-all duration-300 border-2 hover:scale-105 ${selectedCategories.includes(category.value) ? "bg-green-500 text-white border-green-500 shadow-lg" : "bg-dark-surface text-dark-text border-gray-500 hover:bg-dark-hover"}`,
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mr-1", children: category.emoji }),
          category.label
        ]
      },
      category.value
    )) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text-secondary text-sm mt-2 font-medium", children: "选择适合的分类标签，可以选择多个" })
  ] });
}
function EditorForm({
  title,
  onTitleChange,
  children,
  onSubmit,
  isLoading,
  isEditing,
  onClose
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { onSubmit, className: "p-8 space-y-6", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "label",
        {
          htmlFor: "title",
          className: "flex items-center text-lg font-bold text-dark-text mb-3",
          children: "📝 标题"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "text",
          id: "title",
          value: title,
          onChange: (e) => onTitleChange(e.target.value),
          className: "w-full px-6 py-4 border-3 border-gray-500 rounded-2xl focus:outline-none focus:ring-4 focus:ring-dark-primary/30 focus:border-dark-primary bg-dark-surface text-lg font-medium shadow-lg text-dark-text",
          placeholder: "请输入精彩的标题...",
          required: true
        }
      )
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "label",
        {
          htmlFor: "content",
          className: "flex items-center text-lg font-bold text-dark-text mb-3",
          children: "📖 内容"
        }
      ),
      children
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex space-x-4 pt-6", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "flex-1 px-6 py-3 border-3 border-gray-500 text-dark-text rounded-2xl hover:bg-dark-hover transition-all duration-300 font-bold shadow-lg hover:shadow-xl hover:scale-105",
          children: "取消"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "submit",
          disabled: isLoading,
          className: "flex-1 px-6 py-3 bg-gradient-to-r from-dark-success to-dark-primary hover:from-dark-success/80 hover:to-dark-primary/80 text-white rounded-2xl font-bold transition-all duration-300 shadow-lg hover:shadow-xl hover:scale-105 disabled:opacity-50 disabled:scale-100 flex items-center justify-center space-x-2",
          children: isLoading ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "animate-spin rounded-full h-5 w-5 border-2 border-white border-t-transparent" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: isEditing ? "更新中..." : "保存中..." })
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: isEditing ? "✅ 更新经历" : "🚀 发布经历" })
        }
      )
    ] })
  ] });
}
function EditorHeader({
  isEditing,
  isPreviewMode,
  onClose,
  onTogglePreview
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "p-6 border-b-4 border-gray-500 bg-gradient-to-r from-dark-surface to-dark-muted rounded-t-3xl", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center space-x-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          onClick: onClose,
          className: "p-3 hover:bg-dark-hover rounded-full transition-all duration-300 border-2 border-gray-500 bg-dark-card shadow-lg hover:shadow-xl hover:scale-110",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowLeft, { size: 20, className: "text-dark-text" })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-2xl font-bold text-dark-text flex items-center", children: isEditing ? "✏️ 编辑经历" : "✨ 创作新经历" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text-secondary font-medium", children: isEditing ? "修改您的精彩经历" : "记录您的奇思妙想" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "button",
      {
        type: "button",
        onClick: onTogglePreview,
        className: `flex items-center space-x-2 px-4 py-2 rounded-xl font-bold transition-all duration-300 shadow-lg hover:shadow-xl hover:scale-105 ${isPreviewMode ? "bg-gradient-to-r from-dark-success to-dark-primary text-white" : "bg-gradient-to-r from-theme-primary to-theme-secondary text-white"}`,
        children: [
          isPreviewMode ? /* @__PURE__ */ jsxRuntimeExports.jsx(SquarePen, { size: 16 }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Eye, { size: 16 }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: isPreviewMode ? "编辑模式" : "预览模式" })
        ]
      }
    )
  ] }) });
}
function MarkdownPreview({ content }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-full px-6 py-4 border-3 border-t-0 border-gray-500 rounded-b-2xl bg-dark-surface min-h-[500px] overflow-auto shadow-lg", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "prose prose-lg max-w-none", children: content ? /* @__PURE__ */ jsxRuntimeExports.jsx(MarkdownRenderer, { content }) : /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text-secondary italic", children: "预览区域 - 开始输入以查看渲染效果" }) }) });
}
const MAXIMUM_CONCURRENT_UPLOADS = 10;
const MAX_RETRIES = 3;
const BASE_DELAY_MS = 1e3;
const MAX_DELAY_MS = 3e4;
const GATEWAY_VERSION = "v1";
const HASH_ALGORITHM = "SHA-256";
const SHA256_PREFIX = "sha256:";
const DOMAIN_SEPARATOR_FOR_CHUNKS = new TextEncoder().encode("icfs-chunk/");
const DOMAIN_SEPARATOR_FOR_METADATA = new TextEncoder().encode(
  "icfs-metadata/"
);
const DOMAIN_SEPARATOR_FOR_NODES = new TextEncoder().encode("ynode/");
async function withRetry(operation) {
  let lastError;
  for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
    try {
      return await operation();
    } catch (error) {
      lastError = error instanceof Error ? error : new Error(String(error));
      const shouldRetry = isRetriableError(error);
      if (attempt === MAX_RETRIES || !shouldRetry) {
        if (!shouldRetry && attempt < MAX_RETRIES) {
          console.warn(
            `Non-retriable error encountered: ${lastError.message}. Not retrying.`
          );
        }
        throw error;
      }
      const delay = Math.min(
        BASE_DELAY_MS * 2 ** attempt + Math.random() * 1e3,
        MAX_DELAY_MS
      );
      console.warn(
        `Request failed (attempt ${attempt + 1}/${MAX_RETRIES + 1}): ${lastError.message}. Retrying in ${Math.round(delay)}ms...`
      );
      await new Promise((resolve) => setTimeout(resolve, delay));
    }
  }
  throw lastError || new Error("Unknown error occurred during retry attempts");
}
function isRetriableError(error) {
  var _a, _b;
  const errorMessage = ((_a = error == null ? void 0 : error.message) == null ? void 0 : _a.toLowerCase()) || "";
  if ((_b = error == null ? void 0 : error.response) == null ? void 0 : _b.status) {
    const status = error.response.status;
    if (status === 408 || status === 429) return true;
    if (status >= 400 && status < 500) return false;
    if (status >= 500) return true;
  }
  if (errorMessage.includes("ssl") || errorMessage.includes("tls") || errorMessage.includes("network error") || errorMessage.includes("connection") || errorMessage.includes("timeout") || errorMessage.includes("fetch")) {
    return true;
  }
  if (errorMessage.includes("validation") || errorMessage.includes("invalid") || errorMessage.includes("malformed") || errorMessage.includes("unauthorized") || errorMessage.includes("forbidden") || errorMessage.includes("not found")) {
    return false;
  }
  return true;
}
function validateHashFormat(hash, context) {
  if (!hash) {
    throw new Error(`${context}: Hash cannot be empty`);
  }
  if (!hash.startsWith(SHA256_PREFIX)) {
    throw new Error(
      `${context}: Invalid hash format. Expected format: ${SHA256_PREFIX}<64-char-hex>, got: ${hash}`
    );
  }
  const hexPart = hash.substring(SHA256_PREFIX.length);
  if (hexPart.length !== 64) {
    throw new Error(
      `${context}: Invalid hash format. Expected 64 hex characters after ${SHA256_PREFIX}, got ${hexPart.length} characters: ${hash}`
    );
  }
  if (!/^[0-9a-f]{64}$/i.test(hexPart)) {
    throw new Error(
      `${context}: Invalid hash format. Hash must contain only hex characters (0-9, a-f), got: ${hash}`
    );
  }
}
class YHash {
  constructor(bytes) {
    __publicField(this, "bytes");
    if (bytes.length !== 32) {
      throw new Error(`YHash must be exactly 32 bytes, got ${bytes.length}`);
    }
    this.bytes = new Uint8Array(bytes);
  }
  static async fromNodes(left, right) {
    let leftBytes = left instanceof YHash ? left.bytes : new TextEncoder().encode("UNBALANCED");
    let rightBytes = right instanceof YHash ? right.bytes : new TextEncoder().encode("UNBALANCED");
    const combined = new Uint8Array(
      DOMAIN_SEPARATOR_FOR_NODES.length + leftBytes.length + rightBytes.length
    );
    const arrays = [DOMAIN_SEPARATOR_FOR_NODES, leftBytes, rightBytes];
    let offset = 0;
    for (const data of arrays) {
      combined.set(data, offset);
      offset += data.length;
    }
    const hashBuffer = await crypto.subtle.digest(HASH_ALGORITHM, combined);
    return new YHash(new Uint8Array(hashBuffer));
  }
  static async fromChunk(data) {
    return YHash.fromBytes(DOMAIN_SEPARATOR_FOR_CHUNKS, data);
  }
  static async fromHeaders(headers) {
    const headerLines = [];
    for (const [key, value] of Object.entries(headers)) {
      headerLines.push(`${key.trim()}: ${value.trim()}
`);
    }
    headerLines.sort();
    const hash = await YHash.fromBytes(
      DOMAIN_SEPARATOR_FOR_METADATA,
      new TextEncoder().encode(headerLines.join(""))
    );
    return hash;
  }
  static async fromBytes(domainSeparator, data) {
    const combined = new Uint8Array(domainSeparator.length + data.length);
    combined.set(domainSeparator);
    combined.set(data, domainSeparator.length);
    const hashBuffer = await crypto.subtle.digest(HASH_ALGORITHM, combined);
    return new YHash(new Uint8Array(hashBuffer));
  }
  static fromHex(hexString) {
    const bytes = new Uint8Array(
      hexString.match(/.{1,2}/g).map((byte) => Number.parseInt(byte, 16))
    );
    return new YHash(bytes);
  }
  toShaString() {
    return `${SHA256_PREFIX}${this.toHex()}`;
  }
  toString() {
    throw new Error("toString is not supported for YHash");
  }
  toHex() {
    return Array.from(this.bytes).map((b) => b.toString(16).padStart(2, "0")).join("");
  }
}
function nodeToJSON(node) {
  return {
    hash: node.hash.toShaString(),
    left: node.left ? nodeToJSON(node.left) : null,
    right: node.right ? nodeToJSON(node.right) : null
  };
}
class BlobHashTree {
  constructor(chunk_hashes, tree, headers = null) {
    __publicField(this, "tree_type");
    __publicField(this, "chunk_hashes");
    __publicField(this, "tree");
    __publicField(this, "headers");
    this.tree_type = "DSBMTWH";
    this.chunk_hashes = chunk_hashes;
    this.tree = tree;
    if (headers == null) {
      this.headers = [];
    } else if (Array.isArray(headers)) {
      this.headers = headers;
    } else {
      this.headers = Object.entries(headers).map(
        ([key, value]) => `${key.trim()}: ${value.trim()}`
      );
    }
    this.headers.sort();
  }
  static async build(chunkHashes, headers = {}) {
    if (chunkHashes.length === 0) {
      const hex = "8b8e620f084e48da0be2287fd12c5aaa4dbe14b468fd2e360f48d741fe7628a0";
      const bytes = new Uint8Array(Buffer.from(hex, "hex"));
      chunkHashes.push(new YHash(bytes));
    }
    let level = chunkHashes.map((hash) => ({
      hash,
      left: null,
      right: null
    }));
    while (level.length > 1) {
      const nextLevel = [];
      for (let i = 0; i < level.length; i += 2) {
        const left = level[i];
        const right = level[i + 1] || null;
        const parentHash = await YHash.fromNodes(
          left.hash,
          right ? right.hash : null
        );
        nextLevel.push({
          hash: parentHash,
          left,
          right
        });
      }
      level = nextLevel;
    }
    const chunksRoot = level[0];
    if (headers && Object.keys(headers).length > 0) {
      const metadataRootHash = await YHash.fromHeaders(headers);
      const metadataRoot = {
        hash: metadataRootHash,
        left: null,
        right: null
      };
      const combinedRootHash = await YHash.fromNodes(
        chunksRoot.hash,
        metadataRoot.hash
      );
      const combinedRoot = {
        hash: combinedRootHash,
        left: chunksRoot,
        right: metadataRoot
      };
      return new BlobHashTree(chunkHashes, combinedRoot, headers);
    }
    return new BlobHashTree(chunkHashes, chunksRoot, headers);
  }
  toJSON() {
    return {
      tree_type: this.tree_type,
      chunk_hashes: this.chunk_hashes.map((h) => h.toShaString()),
      tree: nodeToJSON(this.tree),
      headers: this.headers
    };
  }
}
class StorageGatewayClient {
  constructor(storageGatewayUrl) {
    this.storageGatewayUrl = storageGatewayUrl;
  }
  getStorageGatewayUrl() {
    return this.storageGatewayUrl;
  }
  async uploadChunk(params) {
    const blobHashString = params.blobRootHash.toShaString();
    const chunkHashString = params.chunkHash.toShaString();
    validateHashFormat(
      blobHashString,
      `uploadChunk[${params.chunkIndex}] blob_hash`
    );
    validateHashFormat(
      chunkHashString,
      `uploadChunk[${params.chunkIndex}] chunk_hash`
    );
    return await withRetry(async () => {
      const queryParams = new URLSearchParams({
        owner_id: params.owner,
        blob_hash: blobHashString,
        chunk_hash: chunkHashString,
        chunk_index: params.chunkIndex.toString(),
        bucket_name: params.bucketName,
        project_id: params.projectId
      });
      const url = `${this.storageGatewayUrl}/${GATEWAY_VERSION}/chunk/?${queryParams.toString()}`;
      const response = await fetch(url, {
        method: "PUT",
        headers: {
          "Content-Type": "application/octet-stream",
          "X-Caffeine-Project-ID": params.projectId
        },
        body: params.chunkData
      });
      if (!response.ok) {
        const errorText = await response.text();
        const error = new Error(
          `Failed to upload chunk ${params.chunkIndex}: ${response.status} ${response.statusText} - ${errorText}`
        );
        error.response = { status: response.status };
        throw error;
      }
      const result = await response.json();
      return {
        isComplete: result.status === "blob_complete"
      };
    });
  }
  async uploadBlobTree(blobHashTree, bucketName, numBlobBytes, owner, projectId, certificateBytes) {
    const treeJSON = blobHashTree.toJSON();
    validateHashFormat(treeJSON.tree.hash, "uploadBlobTree root hash");
    treeJSON.chunk_hashes.forEach((hash, index) => {
      validateHashFormat(hash, `uploadBlobTree chunk_hash[${index}]`);
    });
    return await withRetry(async () => {
      const url = `${this.storageGatewayUrl}/${GATEWAY_VERSION}/blob-tree/`;
      const requestBody = {
        blob_tree: treeJSON,
        bucket_name: bucketName,
        num_blob_bytes: numBlobBytes,
        owner,
        project_id: projectId,
        headers: blobHashTree.headers,
        auth: {
          OwnerEgressSignature: Array.from(certificateBytes)
        }
      };
      const response = await fetch(url, {
        method: "PUT",
        headers: {
          "Content-Type": "application/json",
          "X-Caffeine-Project-ID": projectId
        },
        body: JSON.stringify(requestBody)
      });
      if (!response.ok) {
        const errorText = await response.text();
        const error = new Error(
          `Failed to upload blob tree: ${response.status} ${response.statusText} - ${errorText}`
        );
        error.response = { status: response.status };
        throw error;
      }
    });
  }
}
class StorageClient {
  constructor(actor, bucket, storageGatewayUrl, backendCanisterId, projectId, agent) {
    __publicField(this, "storageGatewayClient");
    this.actor = actor;
    this.bucket = bucket;
    this.backendCanisterId = backendCanisterId;
    this.projectId = projectId;
    this.agent = agent;
    this.storageGatewayClient = new StorageGatewayClient(storageGatewayUrl);
  }
  async getCertificate(hash) {
    const args = encode([Text], [hash]);
    const result = await this.agent.call(this.backendCanisterId, {
      methodName: "_caffeineStorageCreateCertificate",
      arg: args,
      effectiveCanisterId: this.backendCanisterId
    });
    const response = result.response.body;
    if (response && isV4ResponseBody(response)) {
      console.log("Certificate:", response.certificate);
      return response.certificate;
    }
    throw new Error("Expected v4 response body");
  }
  async putFile(path, file, onProgress) {
    if (!path) {
      throw new Error("Path is required");
    }
    const httpHeaders = {
      "Content-Type": "application/json"
    };
    const fileHeaders = {
      "Content-Type": file.type || "application/octet-stream",
      "Content-Disposition": `attachment; filename="${file.name.replace(/"/g, '\\"')}"`,
      "Content-Length": file.size.toString()
    };
    const { chunks, chunkHashes, blobHashTree } = await this.processFileForUpload(file, fileHeaders);
    const blobRootHash = blobHashTree.tree.hash;
    const hashString = blobRootHash.toShaString();
    const certificateBytes = await this.getCertificate(hashString);
    await this.storageGatewayClient.uploadBlobTree(
      blobHashTree,
      this.bucket,
      file.size,
      this.backendCanisterId,
      this.projectId,
      certificateBytes
    );
    await this.parallelUpload(
      chunks,
      chunkHashes,
      blobRootHash,
      httpHeaders,
      onProgress
    );
    validateHashFormat(hashString, `putFile '${hashString}' hash storage`);
    const actorAny = this.actor;
    await actorAny.registerFileReference(path, hashString);
    const url = await this.getDirectURL(path);
    return { path, hash: hashString, url };
  }
  async listObjects() {
    const actorAny = this.actor;
    return await actorAny.listFileReferences();
  }
  async getDirectURL(path) {
    if (!path) {
      throw new Error("Path must not be empty");
    }
    const actorAny = this.actor;
    const fileReference = await actorAny.getFileReference(path);
    validateHashFormat(fileReference.hash, `getDirectURL for path '${path}'`);
    return `${this.storageGatewayClient.getStorageGatewayUrl()}/${GATEWAY_VERSION}/blob/?blob_hash=${encodeURIComponent(fileReference.hash)}&owner_id=${encodeURIComponent(this.backendCanisterId)}&project_id=${encodeURIComponent(this.projectId)}`;
  }
  async processFileForUpload(file, headers) {
    const chunks = this.createFileChunks(file);
    const chunkHashes = [];
    for (let i = 0; i < chunks.length; i++) {
      const chunkData = new Uint8Array(await chunks[i].arrayBuffer());
      const hash = await YHash.fromChunk(chunkData);
      chunkHashes.push(hash);
    }
    const blobHashTree = await BlobHashTree.build(chunkHashes, headers);
    return { chunks, chunkHashes, blobHashTree };
  }
  async parallelUpload(chunks, chunkHashes, blobRootHash, httpHeaders, onProgress) {
    let completedChunks = 0;
    const uploadSingleChunk = async (index) => {
      const chunkData = new Uint8Array(await chunks[index].arrayBuffer());
      const chunkHash = chunkHashes[index];
      await this.storageGatewayClient.uploadChunk({
        blobRootHash,
        chunkHash,
        chunkIndex: index,
        chunkData,
        bucketName: this.bucket,
        owner: this.backendCanisterId,
        projectId: this.projectId,
        httpHeaders
      });
      const currentCompleted = ++completedChunks;
      if (onProgress != null) {
        const percentage = chunks.length === 0 ? 100 : Math.round(currentCompleted / chunks.length * 100);
        onProgress(percentage);
      }
    };
    await Promise.all(
      Array.from(
        { length: MAXIMUM_CONCURRENT_UPLOADS },
        async (_, workerId) => {
          for (let i = workerId; i < chunks.length; i += MAXIMUM_CONCURRENT_UPLOADS) {
            await uploadSingleChunk(i);
          }
        }
      )
    );
  }
  createFileChunks(file, chunkSize = 1024 * 1024) {
    const chunks = [];
    const totalChunks = Math.ceil(file.size / chunkSize);
    for (let index = 0; index < totalChunks; index++) {
      const start = index * chunkSize;
      const end = Math.min(start + chunkSize, file.size);
      const chunk = file.slice(start, end);
      chunks.push(chunk);
    }
    return chunks;
  }
}
const getHttpAgent = async () => {
  const host = "http://localhost:4943";
  const agent = new HttpAgent({
    host
  });
  if (host == null ? void 0 : host.includes("localhost")) {
    await agent.fetchRootKey().catch((err) => {
      console.warn(
        "Unable to fetch root key. Check to ensure that your local replica is running"
      );
      console.error(err);
    });
  }
  return agent;
};
const useFileUpload = () => {
  const { actor } = useActor();
  const [isUploading, setIsUploading] = reactExports.useState(false);
  const { invalidateFileList } = useInvalidateQueries();
  const uploadFile = async (path, data, onProgress) => {
    if (!actor) {
      throw new Error("Backend is not available");
    }
    const bucketName = "default";
    const storageGatewayUrl = "";
    const backendCanisterId = "";
    const projectId = "";
    const storageClient = new StorageClient(
      actor,
      bucketName,
      storageGatewayUrl,
      backendCanisterId,
      projectId,
      await getHttpAgent()
    );
    setIsUploading(true);
    try {
      const res = await storageClient.putFile(path, data, onProgress);
      await invalidateFileList();
      return res;
    } finally {
      setIsUploading(false);
    }
  };
  return { uploadFile, isUploading };
};
const useInvalidateQueries = () => {
  const queryClient = useQueryClient();
  return {
    invalidateFileList: () => queryClient.invalidateQueries({ queryKey: ["fileList"] }),
    invalidateFileUrl: (path) => queryClient.invalidateQueries({ queryKey: ["fileUrl", path] }),
    invalidateAll: () => {
      queryClient.invalidateQueries({ queryKey: ["fileList"] });
      queryClient.invalidateQueries({ queryKey: ["fileUrl"] });
    }
  };
};
function MarkdownToolbar({
  onInsertMarkdown,
  onInsertAtLineStart,
  onInsertCodeBlock,
  onInsertImage
}) {
  const fileInputRef = reactExports.useRef(null);
  const { uploadFile, isUploading } = useFileUpload();
  const [, setUploadProgress] = reactExports.useState(0);
  const handleImageUpload = async (event) => {
    var _a;
    const file = (_a = event.target.files) == null ? void 0 : _a[0];
    if (!file) return;
    if (!file.type.startsWith("image/")) {
      ue.error("请选择图片文件（JPG、PNG、GIF 等）");
      return;
    }
    const maxSize = 10 * 1024 * 1024;
    if (file.size > maxSize) {
      ue.error("图片大小不能超过 10MB");
      return;
    }
    try {
      setUploadProgress(0);
      const path = `articles/images/${Date.now()}_${file.name}`;
      const result = await uploadFile(path, file, (percentage) => {
        setUploadProgress(percentage);
      });
      const imageMarkdown = `![${file.name}](${result.url})`;
      if (onInsertImage) {
        onInsertImage(imageMarkdown);
      } else {
        onInsertMarkdown("\n", "\n", imageMarkdown);
      }
      ue.success("图片上传成功");
    } catch (error) {
      console.error("Image upload failed:", error);
      ue.error(
        error instanceof Error ? error.message : "图片上传失败，请重试"
      );
    } finally {
      setUploadProgress(0);
      if (fileInputRef.current) {
        fileInputRef.current.value = "";
      }
    }
  };
  const triggerFileInput = () => {
    var _a;
    (_a = fileInputRef.current) == null ? void 0 : _a.click();
  };
  const markdownButtons = [
    {
      icon: Bold,
      label: "粗体",
      shortcut: "Ctrl+B",
      action: () => onInsertMarkdown("**", "**", "粗体文本")
    },
    {
      icon: Italic,
      label: "斜体",
      shortcut: "Ctrl+I",
      action: () => onInsertMarkdown("*", "*", "斜体文本")
    },
    {
      icon: Strikethrough,
      label: "删除线",
      action: () => onInsertMarkdown("~~", "~~", "删除线文本")
    },
    {
      icon: Code,
      label: "行内代码",
      shortcut: "Ctrl+`",
      action: () => onInsertMarkdown("`", "`", "代码")
    },
    {
      icon: FileCode,
      label: "代码块",
      shortcut: "Ctrl+Shift+C",
      action: () => onInsertCodeBlock()
    },
    {
      icon: Heading1,
      label: "一级标题",
      action: () => onInsertAtLineStart("# ")
    },
    {
      icon: Heading2,
      label: "二级标题",
      action: () => onInsertAtLineStart("## ")
    },
    {
      icon: Heading3,
      label: "三级标题",
      action: () => onInsertAtLineStart("### ")
    },
    {
      icon: Link,
      label: "链接",
      shortcut: "Ctrl+K",
      action: () => onInsertMarkdown("[", "](https://)", "链接文本")
    },
    {
      icon: Quote,
      label: "引用",
      action: () => onInsertAtLineStart("> ")
    },
    {
      icon: Image,
      label: "图片",
      action: () => triggerFileInput(),
      isLoading: isUploading
    }
  ];
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-dark-surface rounded-t-2xl border-3 border-b-0 border-gray-500 p-3", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center space-x-1 flex-wrap gap-2", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center space-x-1 mr-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-dark-text-secondary text-sm font-medium", children: "Markdown:" }) }),
      markdownButtons.map((button) => {
        const IconComponent = button.icon;
        return /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            onClick: (e) => {
              e.preventDefault();
              button.action();
            },
            disabled: button.isLoading,
            className: "p-2 hover:bg-dark-hover rounded-lg transition-all duration-300 border border-gray-500 bg-dark-card shadow-sm hover:shadow-md hover:scale-105 group disabled:opacity-50 disabled:cursor-not-allowed",
            title: `${button.label}${button.shortcut ? ` (${button.shortcut})` : ""}`,
            children: button.isLoading ? /* @__PURE__ */ jsxRuntimeExports.jsx(
              LoaderCircle,
              {
                size: 16,
                className: "text-dark-text animate-spin"
              }
            ) : /* @__PURE__ */ jsxRuntimeExports.jsx(
              IconComponent,
              {
                size: 16,
                className: "text-dark-text group-hover:text-theme-primary"
              }
            )
          },
          button.label
        );
      }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          ref: fileInputRef,
          type: "file",
          accept: "image/*",
          onChange: handleImageUpload,
          className: "hidden",
          "data-ocid": "markdown.image_input"
        }
      )
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mt-3 pt-3 border-t border-gray-500", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center space-x-4 text-xs text-dark-text-secondary flex-wrap gap-2", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "💡 快捷键:" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Ctrl+B 粗体" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Ctrl+I 斜体" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Ctrl+K 链接" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Ctrl+` 行内代码" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Ctrl+Shift+C 代码块" })
    ] }) })
  ] });
}
function ArticleEditor({
  editingArticle,
  onClose
}) {
  const [title, setTitle] = reactExports.useState("");
  const [content, setContent] = reactExports.useState("");
  const [selectedCategories, setSelectedCategories] = reactExports.useState([]);
  const [isPreviewMode, setIsPreviewMode] = reactExports.useState(false);
  const textareaRef = reactExports.useRef(null);
  const createArticle = useCreateArticle();
  const updateArticle = useUpdateArticle();
  const isEditing = !!editingArticle;
  const isLoading = createArticle.isPending || updateArticle.isPending;
  reactExports.useEffect(() => {
    if (editingArticle) {
      setTitle(editingArticle.title);
      setContent(editingArticle.content);
      setSelectedCategories(editingArticle.categories || []);
    }
  }, [editingArticle]);
  const insertMarkdown = (before, after = "", placeholder = "") => {
    const textarea = textareaRef.current;
    if (!textarea) return;
    const start = textarea.selectionStart;
    const end = textarea.selectionEnd;
    const selectedText = content.substring(start, end);
    const textToInsert = selectedText || placeholder;
    const newText = content.substring(0, start) + before + textToInsert + after + content.substring(end);
    setContent(newText);
    setTimeout(() => {
      if (selectedText) {
        textarea.setSelectionRange(
          start + before.length,
          start + before.length + selectedText.length
        );
      } else {
        textarea.setSelectionRange(
          start + before.length,
          start + before.length + placeholder.length
        );
      }
      textarea.focus();
    }, 0);
  };
  const insertAtLineStart = (prefix) => {
    const textarea = textareaRef.current;
    if (!textarea) return;
    const start = textarea.selectionStart;
    const lines = content.split("\n");
    let currentPos = 0;
    let lineIndex = 0;
    for (let i = 0; i < lines.length; i++) {
      if (currentPos + lines[i].length >= start) {
        lineIndex = i;
        break;
      }
      currentPos += lines[i].length + 1;
    }
    lines[lineIndex] = prefix + lines[lineIndex];
    const newContent = lines.join("\n");
    setContent(newContent);
    setTimeout(() => {
      textarea.setSelectionRange(start + prefix.length, start + prefix.length);
      textarea.focus();
    }, 0);
  };
  const insertCodeBlock = () => {
    const textarea = textareaRef.current;
    if (!textarea) return;
    const start = textarea.selectionStart;
    const end = textarea.selectionEnd;
    const selectedText = content.substring(start, end);
    const codeBlockTemplate = selectedText ? `\`\`\`
${selectedText}
\`\`\`` : "```\n// 在这里输入代码\n```";
    const newText = content.substring(0, start) + codeBlockTemplate + content.substring(end);
    setContent(newText);
    setTimeout(() => {
      if (selectedText) {
        textarea.setSelectionRange(
          start + 4 + selectedText.length,
          start + 4 + selectedText.length
        );
      } else {
        textarea.setSelectionRange(start + 4, start + 4 + 15);
      }
      textarea.focus();
    }, 0);
  };
  const handleKeyDown = (e) => {
    if (e.ctrlKey || e.metaKey) {
      switch (e.key.toLowerCase()) {
        case "b":
          e.preventDefault();
          insertMarkdown("**", "**", "粗体文本");
          break;
        case "i":
          e.preventDefault();
          insertMarkdown("*", "*", "斜体文本");
          break;
        case "k":
          e.preventDefault();
          insertMarkdown("[", "](https://)", "链接文本");
          break;
        case "`":
          e.preventDefault();
          insertMarkdown("`", "`", "代码");
          break;
        case "c":
          if (e.shiftKey) {
            e.preventDefault();
            insertCodeBlock();
          }
          break;
      }
    }
  };
  const handleCategoryToggle = (category) => {
    setSelectedCategories((prev) => {
      if (prev.includes(category)) {
        return prev.filter((c) => c !== category);
      }
      return [...prev, category];
    });
  };
  const handleTogglePreview = () => {
    setIsPreviewMode(!isPreviewMode);
  };
  const handleSubmit = async (e) => {
    e.preventDefault();
    if (!title.trim() || !content.trim()) return;
    try {
      const articleData = {
        title: title.trim(),
        content: content.trim(),
        categories: selectedCategories
      };
      if (isEditing && editingArticle) {
        await updateArticle.mutateAsync({
          articleId: editingArticle.id,
          ...articleData
        });
      } else {
        await createArticle.mutateAsync(articleData);
      }
      onClose();
    } catch (error) {
      console.error("Error saving article:", error);
    }
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "max-w-5xl mx-auto", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-dark-card backdrop-blur-sm rounded-3xl shadow-2xl border-4 border-gray-500", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      EditorHeader,
      {
        isEditing,
        isPreviewMode,
        onClose,
        onTogglePreview: handleTogglePreview
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      EditorForm,
      {
        title,
        onTitleChange: setTitle,
        onSubmit: handleSubmit,
        isLoading,
        isEditing,
        onClose,
        children: [
          !isPreviewMode && /* @__PURE__ */ jsxRuntimeExports.jsx(
            MarkdownToolbar,
            {
              onInsertMarkdown: insertMarkdown,
              onInsertAtLineStart: insertAtLineStart,
              onInsertCodeBlock: insertCodeBlock,
              onInsertImage: (imageMarkdown) => {
                const textarea = textareaRef.current;
                if (!textarea) return;
                const start = textarea.selectionStart;
                const end = textarea.selectionEnd;
                const newText = `${content.substring(0, start)}
${imageMarkdown}
${content.substring(end)}`;
                setContent(newText);
                setTimeout(() => {
                  const newCursorPos = start + imageMarkdown.length + 2;
                  textarea.setSelectionRange(newCursorPos, newCursorPos);
                  textarea.focus();
                }, 0);
              }
            }
          ),
          isPreviewMode ? /* @__PURE__ */ jsxRuntimeExports.jsx(MarkdownPreview, { content }) : /* @__PURE__ */ jsxRuntimeExports.jsx(
            "textarea",
            {
              ref: textareaRef,
              value: content,
              onChange: (e) => setContent(e.target.value),
              onKeyDown: handleKeyDown,
              rows: 20,
              className: "w-full px-6 py-4 border-3 border-t-0 border-gray-500 rounded-b-2xl focus:outline-none focus:ring-4 focus:ring-dark-primary/30 focus:border-dark-primary bg-dark-surface resize-vertical text-lg leading-relaxed shadow-lg text-dark-text font-mono",
              placeholder: `在这里写下您的精彩内容...

支持 Markdown 格式：
**粗体** *斜体* ~~删除线~~
\`行内代码\` [链接](https://example.com)
> 引用文本

# 一级标题
## 二级标题
### 三级标题

\`\`\`
多行代码块
支持语法高亮
保留所有换行和缩进
\`\`\`

- 无序列表项
1. 有序列表项

---

更多格式请参考 Markdown 语法指南`,
              required: true
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            CategorySelector,
            {
              selectedCategories,
              onCategoryToggle: handleCategoryToggle
            }
          )
        ]
      }
    )
  ] }) });
}
function UserInfoEditModal({
  userProfile,
  onClose
}) {
  const [name, setName] = reactExports.useState(userProfile.name);
  const [email, setEmail] = reactExports.useState(userProfile.email ?? "");
  const saveProfile = useSaveUserProfile();
  const handleSubmit = async (e) => {
    e.preventDefault();
    if (!name.trim()) return;
    try {
      await saveProfile.mutateAsync({
        name: name.trim(),
        email: email.trim() || void 0
      });
      onClose();
    } catch (error) {
      console.error("Error saving profile:", error);
    }
  };
  const modalContent = /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: "fixed inset-0 bg-black/70 backdrop-blur-sm flex items-center justify-center p-4",
      style: { zIndex: 999999 },
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-dark-card backdrop-blur-sm rounded-3xl shadow-2xl border-4 border-dark-accent w-full max-w-md", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "p-6 border-b-4 border-dark-border bg-gradient-to-r from-dark-surface to-dark-muted rounded-t-3xl", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center space-x-3", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-12 h-12 bg-gradient-to-r from-dark-primary to-dark-secondary rounded-full flex items-center justify-center shadow-lg", children: /* @__PURE__ */ jsxRuntimeExports.jsx(User, { size: 24, className: "text-white" }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-xl font-bold text-dark-text", children: "✏️ 编辑用户信息" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text-secondary font-medium", children: "修改您的个人资料" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              type: "button",
              onClick: onClose,
              disabled: saveProfile.isPending,
              className: "p-2 hover:bg-dark-hover rounded-full transition-all duration-300 border-2 border-dark-accent bg-dark-card shadow-lg hover:shadow-xl hover:scale-110 disabled:opacity-50 disabled:transform-none",
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { size: 18, className: "text-dark-text" })
            }
          )
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { onSubmit: handleSubmit, className: "p-8 space-y-6", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-20 h-20 rounded-full bg-gradient-to-br from-dark-primary to-dark-secondary flex items-center justify-center shadow-2xl border-4 border-dark-accent mx-auto mb-4", children: /* @__PURE__ */ jsxRuntimeExports.jsx(User, { size: 40, className: "text-white" }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text-secondary font-medium", children: "您的头像" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-3", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "label",
              {
                htmlFor: "user-name",
                className: "flex items-center text-lg font-bold text-dark-text",
                children: "✨ 用户名称"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                id: "user-name",
                type: "text",
                value: name,
                onChange: (e) => setName(e.target.value),
                className: "w-full px-6 py-4 border-3 border-dark-border rounded-2xl focus:outline-none focus:ring-4 focus:ring-dark-primary/30 focus:border-dark-primary bg-dark-surface text-lg font-medium shadow-lg text-dark-text",
                placeholder: "请输入您的用户名称",
                required: true
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-3", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "label",
              {
                htmlFor: "user-email",
                className: "flex items-center text-lg font-bold text-dark-text",
                children: [
                  "📧 邮箱",
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ml-2 text-sm font-medium text-dark-text-secondary", children: "（选填，用于接收文章隐藏通知）" })
                ]
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                id: "user-email",
                type: "email",
                value: email,
                onChange: (e) => setEmail(e.target.value),
                className: "w-full px-6 py-4 border-3 border-dark-border rounded-2xl focus:outline-none focus:ring-4 focus:ring-dark-primary/30 focus:border-dark-primary bg-dark-surface text-lg font-medium shadow-lg text-dark-text",
                placeholder: "可选，例如 you@example.com",
                "data-ocid": "user.email.input"
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "bg-gradient-to-r from-dark-primary/20 to-dark-secondary/20 rounded-2xl p-4 border-3 border-dark-primary/30", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center space-x-3", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-2xl", children: "💡" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text font-medium", children: "这个名称将在您的经历和排行榜中显示" })
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex space-x-4 pt-4", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                type: "button",
                onClick: onClose,
                disabled: saveProfile.isPending,
                className: "flex-1 px-6 py-3 border-3 border-dark-border text-dark-text rounded-2xl hover:bg-dark-hover transition-all duration-300 font-bold shadow-lg hover:shadow-xl hover:scale-105 disabled:opacity-50 disabled:transform-none",
                children: "取消"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                type: "submit",
                disabled: saveProfile.isPending || !name.trim(),
                className: "flex-1 px-6 py-3 bg-gradient-to-r from-dark-success to-dark-primary hover:from-dark-success/80 hover:to-dark-primary/80 text-white rounded-2xl font-bold transition-all duration-300 shadow-lg hover:shadow-xl hover:scale-105 disabled:opacity-50 disabled:transform-none flex items-center justify-center space-x-2",
                children: saveProfile.isPending ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "animate-spin rounded-full h-4 w-4 border-2 border-white border-t-transparent" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "保存中..." })
                ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Save, { size: 16 }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "✅ 保存" })
                ] })
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "p-4 bg-gradient-to-r from-dark-success/10 to-dark-primary/10 border-t-4 border-dark-border rounded-b-3xl", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-center", children: /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text-secondary text-sm font-medium", children: "🎉 更新信息后将立即生效" }) }) })
      ] })
    }
  );
  return typeof document !== "undefined" ? reactDomExports.createPortal(modalContent, document.body) : null;
}
function ProfilePage({ navigate }) {
  const { data: userProfile, isLoading } = useGetCallerUserProfile();
  const { data: myArticles } = useGetMyArticles();
  const { data: loggingEnabled } = useGetMiningLoggingEnabled();
  const [showEditModal, setShowEditModal] = reactExports.useState(false);
  const [showArticleEditor, setShowArticleEditor] = reactExports.useState(false);
  const [editingArticle, setEditingArticle] = reactExports.useState(void 0);
  const handleBack = () => {
    navigate("/");
  };
  const handleEditProfile = () => {
    setShowEditModal(true);
  };
  const handleCloseEditModal = () => {
    setShowEditModal(false);
  };
  const handleCreateArticle = () => {
    setEditingArticle(void 0);
    setShowArticleEditor(true);
  };
  const handleEditArticle = (id, title, content, coverImage, categories) => {
    setEditingArticle({
      id,
      title,
      content,
      coverImage,
      categories
    });
    setShowArticleEditor(true);
  };
  const handleReadArticle = (article) => {
    if (loggingEnabled) {
      console.log("ProfilePage: 准备导航到文章阅读页面", {
        articleId: article.id.toString(),
        title: article.title
      });
    }
    navigate("/article", { articleId: article.id.toString() });
  };
  const handleCloseEditor = () => {
    setShowArticleEditor(false);
    setEditingArticle(void 0);
  };
  if (isLoading) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-center py-16", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-dark-card backdrop-blur-sm rounded-3xl p-12 border-4 border-gray-500 shadow-2xl inline-block", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "animate-bounce rounded-full h-12 w-12 bg-gradient-to-r from-dark-primary to-dark-secondary mx-auto mb-6 flex items-center justify-center", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-white text-xl", children: "👤" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text text-xl font-bold", children: "加载个人页面中..." })
    ] }) });
  }
  if (showArticleEditor) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ArticleEditor,
      {
        editingArticle,
        onClose: handleCloseEditor
      }
    );
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-4xl mx-auto space-y-8", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-dark-card backdrop-blur-sm rounded-3xl shadow-2xl border-4 border-gray-500", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "p-6 border-b-4 border-gray-500 bg-gradient-to-r from-dark-surface to-dark-muted rounded-t-3xl", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center space-x-4", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            onClick: handleBack,
            className: "p-3 hover:bg-dark-hover rounded-full transition-all duration-300 border-2 border-gray-500 bg-dark-card shadow-lg hover:shadow-xl hover:scale-110",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowLeft, { size: 20, className: "text-dark-text" })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-2xl font-bold text-dark-text flex items-center", children: "👤 个人页面" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text-secondary font-medium", children: "管理您的个人信息和经历内容" })
        ] })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-8 space-y-8", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-xl font-bold text-dark-text flex items-center", children: "✨ 用户信息" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              type: "button",
              onClick: handleEditProfile,
              className: "w-full bg-gradient-to-r from-dark-surface to-dark-muted rounded-2xl border-3 border-gray-500 p-6 shadow-lg hover:shadow-xl transition-all duration-300 hover:scale-[1.02] text-left group",
              children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center space-x-4", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-16 h-16 rounded-full bg-gradient-to-br from-dark-primary to-dark-secondary flex items-center justify-center shadow-lg", children: /* @__PURE__ */ jsxRuntimeExports.jsx(User, { size: 32, className: "text-white" }) }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-lg font-bold text-dark-text mb-1", children: (userProfile == null ? void 0 : userProfile.name) || "未设置" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text-secondary font-medium", children: "点击编辑用户信息" })
                  ] })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-10 h-10 bg-gradient-to-r from-dark-primary to-dark-secondary rounded-full flex items-center justify-center shadow-lg group-hover:scale-110 transition-transform duration-300", children: /* @__PURE__ */ jsxRuntimeExports.jsx(SquarePen, { size: 20, className: "text-white" }) })
              ] })
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-xl font-bold text-dark-text flex items-center", children: "📝 我的经历" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "button",
              {
                type: "button",
                onClick: handleCreateArticle,
                className: "bg-gradient-to-r from-dark-success to-dark-primary hover:from-dark-success/80 hover:to-dark-primary/80 text-white px-6 py-3 rounded-2xl font-bold transition-all duration-300 shadow-lg hover:shadow-xl hover:scale-105 flex items-center space-x-2",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(PenTool, { size: 16 }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "✨ 创作新经历" })
                ]
              }
            )
          ] }),
          myArticles && myArticles.length > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(
            ArticleList,
            {
              articles: myArticles,
              showAuthor: false,
              onEdit: handleEditArticle,
              onRead: handleReadArticle,
              isOwner: true
            }
          ) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-center py-12", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-dark-surface backdrop-blur-sm rounded-3xl p-8 border-3 border-gray-500 shadow-lg inline-block", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-16 h-16 bg-gradient-to-br from-dark-primary to-dark-secondary rounded-full flex items-center justify-center mx-auto mb-4 shadow-lg", children: /* @__PURE__ */ jsxRuntimeExports.jsx(BookOpen, { size: 32, className: "text-white" }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text text-lg font-bold mb-2", children: "🎨 您还没有发布任何经历" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-dark-text-secondary", children: "点击上方按钮开始创作吧！" })
          ] }) })
        ] })
      ] })
    ] }),
    showEditModal && userProfile && /* @__PURE__ */ jsxRuntimeExports.jsx(
      UserInfoEditModal,
      {
        userProfile,
        onClose: handleCloseEditModal
      }
    )
  ] });
}
export {
  ProfilePage as default
};
